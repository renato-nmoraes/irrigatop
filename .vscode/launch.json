{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Flask Docker Container",
      "type": "python",
      "request": "attach",
      "port": 5678,
      "host": "localhost",
      "jinja": true,
      "justMyCode": false,
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/web",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}
